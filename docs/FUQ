
Вылет VS Code убьет и запущенный Blender
Blender на интегрированном терминале в VS Code

Двойной запуск Blender заблокирует и уведомит пользователя


Устаревшие конфиги могут не позволить мне найти Python интерпретатор в папки конфигов

При добавлении нового проекта в текущее рабочее пространстве
VS Code отрывает терминал Blender от текущей сессии.

Нет НиНада :)
Работать нужно в каталоге с проектами для разработки, а не в любом каталоге.

Обрабатываются:
 - скрипт обычный
 - скрипт с register, без __name__ == "__main__"
 - многофайловыйы аддон


У меня явно всё разрегистрируется, но что-то разрегивается избыточно.
Нужно сделать список с проверкой классов.
Я буду в него добавлять классы для исключения и проверять когда вылетает.

        for name in dir(module):
            obj = getattr(module, name)
            if isinstance(obj, type) and name not in processed_classes and not issubclass(obj, bpy.types.PropertyGroup):
                # Проверяем, есть ли у класса атрибут bl_rna
                if hasattr(obj, "bl_rna"):
                    print('  ****  :', obj)

Сделай вариант с проверкой элементов из списка, а не просто конкретного bpy.types.PropertyGroup